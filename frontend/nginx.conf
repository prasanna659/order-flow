server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback -- any path that isn't a real file goes to index.html
    # so client-side navigation doesn't 404 on refresh.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
